#!/usr/bin/env bash
# Placeholder — implemented in step 12 (signing, notarization, distribution).
# Referenced by .github/workflows/release.yml so the contract is visible early.
set -euo pipefail
echo "scripts/build-release.sh is not implemented yet — see .scratch/plan/agentbar/steps/12-distribution.md" >&2
exit 1

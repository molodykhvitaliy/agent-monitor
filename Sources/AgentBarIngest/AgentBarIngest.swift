// AgentBarIngest
//
// Loopback ingest: the local endpoint both providers deliver events to, its
// bearer-token authentication and its event decoding. Keeps a synchronous
// response path so the Approve/Deny backlog item needs no transport change.
//
// Filled in by step 03. This file reserves the module boundary so the
// dependency graph in Package.swift is real from the first commit.

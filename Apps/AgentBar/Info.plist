<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>AgentBar</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<!-- The icon actool compiles from Apps/AgentBar/AgentBar.icon. Declared by
	     name rather than as a CFBundleIconFile: a layered icon has no single
	     .icns to point at, and the name is what Launch Services resolves
	     against the compiled Assets.car. LSUIElement means there is no Dock
	     icon, but the icon is far from unused — Finder, Spotlight, the login
	     items list, System Settings › Notifications and the leading slot of
	     every banner all show it. -->
	<key>CFBundleIconName</key>
	<string>AgentBar</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<!-- Declared even though LSUIElement means the app never appears in the Dock
	     or in Launchpad: Finder's Get Info panel, Spotlight and the App Store's
	     own category vocabulary all read it, and a distributed application with
	     no category reads as unfinished. -->
	<key>LSApplicationCategoryType</key>
	<string>public.app-category.developer-tools</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<!-- Menu-bar only: no Dock icon, no application menu. The status item is the
	     entire interface, which is why it must always offer a way to quit. -->
	<key>LSUIElement</key>
	<true/>
	<!-- AgentBarMain creates NSApplication.shared itself rather than going
	     through NSApplicationMain, so nothing reads this today. It is declared
	     because Launch Services and AppKit consult it, and its absence is a
	     latent surprise for whoever changes the entry point. -->
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<key>NSHumanReadableCopyright</key>
	<string>© 2026 Vitaliy Molodykh. Licensed under the Apache License, Version 2.0.</string>
</dict>
</plist>
